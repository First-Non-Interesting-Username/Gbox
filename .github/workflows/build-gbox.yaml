name: Build Gbox

on: 
    schedule:
    - cron: '0 16 * * *'
    workflow_dispatch:

jobs: 
    build-push:
        name: Build images and push to ghcr.io
        runs-on: ubuntu-latest
        permissions:
            contents: read
            packages: write
            id-token: write
        strategy:
            fail-fast: false
            matrix:
                image_name: [gbox-plasma, gbox-gnome]
        
        steps:
            - name: Free disk space
              uses: jlumbroso/free-disk-space@54081f138730dfa15788a46383842cd2f914a1be # v1.3.1
              with:
                tool-cache: true
                android: true
                dotnet: true
                haskell: true
                large-packages: true
                docker-images: true
                swap-storage: true

            - name: checkout
              uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
            
            - name: Matrix Variables
              run: |
                echo "IMAGE_NAME=${{ matrix.image_name }}" >> $GITHUB_ENV
            
            - name: Generate tags
              id: tags
              shell: bash
              run: |
                TIMESTAMP="$(date +%Y%m%d)"
                TAGS=("${TIMESTAMP}")
                TAGS+=("latest")
                OUTPUT_TAGS="${TAGS[*]}"
                echo "tags=${OUTPUT_TAGS}" >> $GITHUB_OUTPUT

            - name: Image Metadata
              uses: docker/metadata-action@80c7e94dd9b9319bd5eb7a0e0fe9291e23a2a2e9 # v6
              id: meta
              with:
                images: |
                    ${{ env.IMAGE_NAME }}
                labels: |
                    org.opencontainers.image.title=Gbox - Gaming distroBOX
                    org.opencontainers.image.description=Image designed for gaming in distrobox
                    org.opencontainers.image.vendor=${{ github.repository_owner }}
                    org.opencontainers.image.authors=${{ github.repository_owner }}
                    org.opencontainers.image.base.name=ghcr.io/ublue-os/arch-toolbox


            - name: Compute SOURCE_DATE_EPOCH
              id: epoch
              run: echo "epoch=$(date -d '${{ github.event.head_commit.timestamp }}' +%s)" >> $GITHUB_OUTPUT

            - name: Build Image
              id: build_image
              uses: redhat-actions/buildah-build@7a95fa7ee0f02d552a32753e7414641a04307056 # v2
              with:
                containerfiles: |
                    ./Containerfile.gbox
                image: ${{ env.IMAGE_NAME }}
                tags: |
                    ${{ steps.tags.outputs.tags }}
                labels: ${{ steps.meta.outputs.labels }}
                oci: false
                extra-args: |
                    --target=${{ matrix.image_name }} --security-opt=seccomp=unconfined
                    --skip-unused-stages=false
                build-args: |
                    SOURCE_DATE_EPOCH=${{ steps.epoch.outputs.epoch }}

            - name: Login to ghcr.io
              uses: redhat-actions/podman-login@4934294ad0449894bcd1e9f191899d7292469603 # v1
              with:
                registry: ghcr.io
                username: ${{ github.actor }}
                password: ${{ secrets.GITHUB_TOKEN }}

            - name: Lowercase registry owner
              run: echo "OWNER=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_ENV

            - name: Push to ghcr.io
              id: push
              uses: redhat-actions/push-to-registry@5ed88d269cf581ea9ef6dd6806d01562096bee9c # v2
              with:
                image: ${{ steps.build_image.outputs.image }}
                tags: ${{ steps.build_image.outputs.tags }}
                registry: ghcr.io/${{ env.OWNER }}
                username: ${{ github.actor }}
                password: ${{ secrets.GITHUB_TOKEN }}

            - name: Install Cosign
              uses: sigstore/cosign-installer@398d4b0eeef1380460a10c8013a76f728fb906ac # v3
            
            - name: Sign image with Cosign
              run: |
                cosign sign --yes \
                ghcr.io/${{ env.OWNER }}/${{ env.IMAGE_NAME }}@${{ steps.push.outputs.digest }}
